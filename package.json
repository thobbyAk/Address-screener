{
  "name": "elliptic-screener",
  "version": "1.0.0",
  "description": "CLI script to screen an Ethereum address and output a CSV with compliance-relevant signals",
  "main": "src/index.ts",
  "scripts": {
    "screen": "ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
